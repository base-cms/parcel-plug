{{#each ads as |ad|}}
  {{lineitem/-ads/item
    activeId=activeId
    id=ad.id
    title=(concat ad.name " (" ad.size ")")
    parentId=elementId
    isActionRunning=isActionRunning
    toggle=(action "toggle")
  }}
{{/each}}
