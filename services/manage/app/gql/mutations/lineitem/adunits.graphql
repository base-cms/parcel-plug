mutation LineItemAdUnits($input: LineItemAdUnitsMutationInput!) {
  lineitemAdUnits(input: $input) {
    id
    status
    requires
    targeting {
      adunits {
        id
        fullName
      }
    }
  }
}
