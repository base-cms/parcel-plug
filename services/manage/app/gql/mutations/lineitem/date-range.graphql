mutation LineItemDateRange($input: LineItemDateRangeMutationInput!) {
  lineitemDateRange(input: $input) {
    id
    status
    requires
    dates {
      type
      start
      end
      days
    }
  }
}
