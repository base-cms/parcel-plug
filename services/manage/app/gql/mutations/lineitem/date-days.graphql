mutation LineItemDateDays($input: LineItemDateDaysMutationInput!) {
  lineitemDateDays(input: $input) {
    id
    status
    requires
    dates {
      type
      start
      end
      days
    }
  }
}
