mutation LineItemPublishers($input: LineItemPublishersMutationInput!) {
  lineitemPublishers(input: $input) {
    id
    status
    requires
    targeting {
      publishers {
        id
        name
      }
    }
  }
}
