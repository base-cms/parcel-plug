mutation AdUnitDeployment($input: AdUnitDeploymentMutationInput!) {
  adunitDeployment(input: $input) {
    id
    deployment {
      id
      name
      publisher {
        id
        name
      }
    }
  }
}
